version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: first_db
      POSTGRES_USER: wisely
      POSTGRES_PASSWORD: zzzzzz